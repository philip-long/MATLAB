function [c,ceq,c_grad,ceq_grad] = gammaCost(q)
%gammaConstraint Constrain the hyperplanes with gradient

Gamma_p_gradient=cell(length(active_joint),1);
Gamma_m_gradient=cell(length(active_joint),1);


end

